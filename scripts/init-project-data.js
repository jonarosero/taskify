// Taskify no longer seeds project data in the repository.
// User projects are created inside the workspace chosen on first launch.
console.log('Taskify uses a user-selected workspace. No repository data initialization is required.')
